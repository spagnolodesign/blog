<p id="notice"><%= notice %></p>

<h1>Articles</h1>

<%= cl_image_tag("j5fvvzgjj9p94xpvka78", width: 400, height: 300) %>
<%= cl_image_tag("gan8archr9rock65aytw", width: 150, height: 150, crop: :thumb, gravity: :face) %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
        <td><%= link_to 'Show', article %></td>
        <td><%= link_to 'Edit', edit_article_path(article) %></td>
        <td><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>
